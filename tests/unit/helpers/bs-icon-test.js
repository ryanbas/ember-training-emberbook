import { bsIcon } from '../../../helpers/bs-icon';
import { module, test } from 'qunit';

module('Unit | Helper | bs icon');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = bsIcon(42);
  assert.ok(result);
});
